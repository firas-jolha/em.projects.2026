\section{Results}
\label{S:results}

% --- Replace everything below with your own content. ---

\Cref{T:example} in \Cref{APX:examples} illustrates the table format we use to report results; replace it with your actual findings here (or move a copy of the example table code into this section and update the numbers).

\textit{[Describe your key result in words first, then support it with statistics, e.g.:]} The proposed group showed a higher mean outcome ($M = \textit{[value]}$, $SD = \textit{[value]}$) than the baseline group ($M = \textit{[value]}$, $SD = \textit{[value]}$). This difference was \textit{[statistically significant / not statistically significant]}, $t(\textit{[df]}) = \textit{[t-value]}$, $p = \textit{[p-value]}$, with an effect size of $d = \textit{[value]}$.

\Cref{F:example} in \Cref{APX:examples} shows the corresponding figure format; replace the placeholder plot with your own visualization (e.g., a boxplot of the two groups, a scatter plot with a fitted regression line, or a bar chart of proportions).

If you ran multiple tests, summarize them together, e.g., in a single table with one row per hypothesis/comparison, including the test statistic, degrees of freedom, $p$-value, and effect size for each.

\textit{[Report results neutrally here — save interpretation, caveats, and comparison with prior work for \Cref{S:discussion}.]}